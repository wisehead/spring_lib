#1.dkeys

```
dkeys
--List<DefaultKeyConfig> list = defaultKeyConfigService.findByExample(example);
--List<String> keys = defaultKeyConfigs.stream().map(DefaultKeyConfig::getDefaultKey).collect(Collectors.toList());
--return WrapMapper.ok().result(keys);

```
